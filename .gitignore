# Python 构建脚本产生的缓存
__pycache__/
*.py[cod]
.pytest_cache/

# 编辑器 / 系统
.DS_Store
Thumbs.db
.vscode/
.idea/

# 本地工具配置（因人而异，不进仓库）
.claude/

# 构建源与内部文档：不进仓库（本地保留，换机器请从备份拿）
# _src/ 是 md 源 + 构建器 + 校验脚本；CLAUDE.md 是写给 agent 的内部交接文档
_src/
CLAUDE.md
