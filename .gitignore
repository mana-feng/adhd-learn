# ─────────────────────────────────────────────────────────────
# 划线原则：**站点跑得起来所必需的一律进仓库，agent 内部协作产物一律不进。**
# ⚠️ 2026-08-20 发现过一次反例：assets/ 下四个 JS 被 474 个页面引用，
#    却从来没进过仓库 —— clone 下来的站点是坏的。所以下面不写 `assets/` 整目录，
#    只排除明确该排除的东西。
# ─────────────────────────────────────────────────────────────

# Python 构建脚本产生的缓存
__pycache__/
*.py[cod]
.pytest_cache/

# 编辑器 / 系统
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
*~

# 本地工具配置（因人而异，不进仓库）
.claude/

# 构建源与内部文档：不进仓库（本地保留，换机器请从备份拿）
# _src/ 是 md 源 + 构建器 + 校验脚本；CLAUDE.md 是写给 agent 的内部交接文档
# ⚠️⚠️ 提醒（不是改动）：CLAUDE.md 自己记录的头号事故就是「源码只有一份、丢了就重建不了」。
#     现在 _src/（22 MB，md 源 + 构建器 + 三十多个校验脚本）**只存在于本机和 OneDrive**。
#     要不要把它纳入版本控制，是一个需要人来定的决定 —— 这里保持原样，不擅自反转。
_src/
CLAUDE.md

# agent 之间的协作产物：讨论稿、方针稿、经验库
# ⭐ 和 CLAUDE.md 同一类 —— 它们是「怎么做这个站」的过程记录，不是站点本身
AGENTS.md
CONTENT_DESIGN_DISCUSSION.md
CONTENT_DESIGN_PRINCIPLES.md
.learnings/

# 临时与备份文件
*.bak
*.tmp
*.log
*.orig
*.rej
watch_store/
mine.*.md5

# 依赖目录（本项目目前是纯静态站，留着防止将来引入时误提交）
node_modules/
.venv/
venv/

# 机密：绝不进仓库
# ⚠️ CLAUDE.md 里那条「删掉代码里的 key 不等于删掉它 —— git 历史里还在」
#    说的就是这一类，所以宁可多列几条
.env
.env.*
!.env.example
*.pem
*.key
secrets.*
